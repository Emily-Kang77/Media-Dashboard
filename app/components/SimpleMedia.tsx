import React from 'react'

/**
 * SimpleMedia just accepts one podcast as a prop.
 * This is a card with the link, and description.
 * And some kind of picture with it. (Later)
 * @returns 
 */
const SimpleMedia = () => {
  return (
    <div>SimpleMedia</div>
  )
}

export default SimpleMedia